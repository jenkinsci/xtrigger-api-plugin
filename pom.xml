<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.jenkins-ci.plugins</groupId>
        <artifactId>plugin</artifactId>
        <version>3.2</version>
    </parent>

    <groupId>org.jenkins-ci.lib</groupId>
    <artifactId>xtrigger-lib</artifactId>
    <version>0.34-SNAPSHOT</version>
    <packaging>jar</packaging>
    <name>Jenkins XTrigger Library</name>
    <url>https://wiki.jenkins-ci.org/display/JENKINS/XTrigger+Plugin</url>

    <licenses>
        <license>
            <name>MIT license</name>
            <comments>All source code is under the MIT license.</comments>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>gbois</id>
            <name>Gregory Boissinot</name>
            <timezone>+1</timezone>
            <roles>
                <role>Project Owner</role>
                <role>Project Lead Developer</role>
            </roles>
        </developer>
    </developers>

    <properties>
        <java.level>7</java.level>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jenkins.core.version>2.60.1</jenkins.core.version>
        <envinject-lib.version>1.30-SNAPSHOT</envinject-lib.version>
        <!-- FIXME make findbugs obligatory after migration to piplined xtrigger -->
        <findbugs.failOnError>false</findbugs.failOnError>
    </properties>

    <scm>
        <connection>scm:git:git://github.com/jenkinsci/xtrigger-lib.git</connection>
        <developerConnection>scm:git:git@github.com:jenkinsci/xtrigger-lib.git</developerConnection>
      <tag>HEAD</tag>
  </scm>

    <dependencies>

        <!--<dependency>-->
            <!--<groupId>org.jenkins-ci.main</groupId>-->
            <!--<artifactId>jenkins-core</artifactId>-->
            <!--<version>${jenkins.core.version}</version>-->
            <!--<scope>provided</scope>-->
        <!--</dependency>-->

        <dependency>
            <groupId>org.jenkins-ci.lib</groupId>
            <artifactId>envinject-lib</artifactId>
            <version>${envinject-lib.version}</version>
        </dependency>

        <!--<dependency>-->
            <!--<groupId>org.jenkins-ci.main</groupId>-->
            <!--<artifactId>jenkins-test-harness</artifactId>-->
            <!--<version>${jenkins.core.version}</version>-->
        <!--</dependency>-->

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifestEntries>
                            <Jenkins-ClassFilter-Whitelisted>true</Jenkins-ClassFilter-Whitelisted>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>repo.jenkins-ci.org</id>
            <name>Jenkins Repository</name>
            <url>http://repo.jenkins-ci.org/public/</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>repo.jenkins-ci.org</id>
            <url>http://repo.jenkins-ci.org/public/</url>
        </pluginRepository>
    </pluginRepositories>

</project>
